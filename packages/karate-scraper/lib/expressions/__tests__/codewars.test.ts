// import * as scraper from '../../scraper'
// import {
//   expression as katasExpression,
//   url as katasAddr,
// } from '../codewars/katas'

test('Scrape katas', async () => {
  // const url = katasAddr({ userName: 'SiarheiMelnik' })
  // const expression = katasExpression()
  // const data = await scraper.scrape({ url, expression })
  // const value = data.result.value
  // expect(JSON.parse(value)).toContain('multiply')
})
