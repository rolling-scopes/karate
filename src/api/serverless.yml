service: karate-service-api

provider:
  name: aws
  runtime: nodejs6.10
  region: eu-west-1
  stage: test
  iamRoleStatements:
    - Effect: Allow
      Action:
      - dynamodb:*
      Resource: arn:aws:dynamodb:*:*:table/*
  environment:
    region: ${self:provider.region}
    task_table: ${self:custom.task_table}
    result_table: ${self:custom.result_table}

plugins:
  - serverless-webpack

custom:
  webpack: '../../webpack.config.js'
  webpackIncludeModules:
    packagePath: '../../package.json'
  stage: ${opt:stage, self:provider.stage}
  prefix: ${self:custom.stage}-${self:service}
  result_arn: ${file(../../config.yml):result_arn}
  task_table: ${self:custom.prefix}-task
  result_table: ${self:custom.prefix}-result

functions:
  addTask:
    handler: handler.addTask
    name: ${self:custom.prefix}-addTask
    events:
      - http:
          method: post
          path: tasks/add
          cors: true

  findExpression:
    handler: handler.findExpression
    name: ${self:custom.prefix}-findExpression
    events:
      - http:
          method: post
          path: expression
          cors: true

  getResult:
    handler: handler.getResult
    name: ${self:custom.prefix}-getResult
    events:
      - http:
          method: get
          path: results/{task_id}
          cors: true

  updateResult:
    handler: handler.updateResult
    name: ${self:custom.prefix}-updateResult
    events:
      - sns: ${self:custom.result_arn}

resources:
  Resources:
    ResultTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:provider.environment.result_table}
        KeySchema:
          -
            AttributeName: id
            KeyType: HASH
        AttributeDefinitions:
          -
            AttributeName: id
            AttributeType: S
        ProvisionedThroughput:
          ReadCapacityUnits: 1
          WriteCapacityUnits: 1
    TaskTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:provider.environment.task_table}
        KeySchema:
          -
            AttributeName: id
            KeyType: HASH
        AttributeDefinitions:
          -
            AttributeName: id
            AttributeType: S
        ProvisionedThroughput:
          ReadCapacityUnits: 1
          WriteCapacityUnits: 1
        StreamSpecification:
          StreamViewType: NEW_IMAGE
